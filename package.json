{
  "name": "space-maze",
  "type": "module",
  "version": "1.0.0",
  "description": "Space maze explorer built with Astro and Three.js.",
  "packageManager": "bun@1.3.10",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro",
    "deploy": "bun run build && bunx wrangler pages deploy dist --project-name space-maze",
    "deploy:production": "bun run build && bunx wrangler pages deploy dist --project-name space-maze",
    "lint": "oxlint",
    "format": "oxfmt",
    "prepare": "husky"
  },
  "dependencies": {
    "astro": "^6.0.1",
    "three": "^0.183.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.7",
    "typescript": "^5.9.3",
    "oxlint": "^1.52.0",
    "oxfmt": "^0.37.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.3"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "oxlint --fix",
      "oxfmt"
    ]
  }
}
