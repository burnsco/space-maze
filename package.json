{
  "name": "space-maze",
  "version": "1.0.0",
  "description": "Space maze explorer built with Astro and Three.js.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "bun run build && wrangler dev --config dist/server/wrangler.json",
    "check": "astro check",
    "astro": "astro",
    "deploy": "bun run build && wrangler deploy --config dist/server/wrangler.json",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.10",
    "astro": "^6.1.8",
    "three": "^0.184.0",
    "wrangler": "^4.84.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "typescript": "^6.0.3"
  }
}
